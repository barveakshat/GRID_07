spring:
  application:
    name: GRID_07
  datasource:
    url: jdbc:postgresql://localhost:5432/grid07db
    username: akshat
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6379

app:
  notification:
    sweep-cron: "0 */5 * * * *"
